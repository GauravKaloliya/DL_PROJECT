# =====================================================
# C.O.G.N.I.T. Backend Environment Variables
# =====================================================

# Database Configuration (REQUIRED for production)
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database?sslmode=require
# Get this from your database provider (Neon, Render, AWS RDS, etc.)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Flask Configuration (REQUIRED for production)
# Generate a strong secret key: openssl rand -hex 32
SECRET_KEY=change-this-to-a-random-secret-key-in-production
FLASK_DEBUG=0

# CORS Configuration
# Controls which frontend domains can access the API
#
# OPTION 1: Same-origin deployment (Vercel combined deployment)
# - Leave empty or set to your main domain
# - Both frontend and backend served from same origin
# - CORS handling is minimal since requests are same-origin
# CORS_ORIGINS=https://your-app.vercel.app
#
# OPTION 2: Separate deployments (different domains)
# - Set to your frontend domain(s)
# - Multiple domains can be comma-separated
# CORS_ORIGINS=https://your-frontend.vercel.app,https://your-frontend.com
#
# OPTION 3: Development only
# - Leave empty to use default localhost origins
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
CORS_ORIGINS=

# Application Configuration
MIN_WORD_COUNT=60
TOO_FAST_SECONDS=5
IP_HASH_SALT=change-this-to-a-random-salt-in-production

# Platform Detection (set automatically by Vercel)
# VERCEL=1

# Server Configuration
# PORT is set automatically by Render.com and Vercel
# Uncomment for local development
# PORT=5000